<h1 class="d-flex justify-content-center mt-3"> {{ form_title }}</h1>     
<div class="row">
    <div class="col col-12 d-flex justify-content-center"> 
        <div class="mt-2">
        {% block fom_body %}
            {{ form_start(form, {'attr': {'novalidate':'novalidate'}}) }}
                {{ form_errors(form) }}
                {{ form(form) }}
            {{ form_end(form)}}  
        {% endblock %}
        </div>        
    </div>
</div> 